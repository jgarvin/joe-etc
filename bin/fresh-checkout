#!/usr/bin/env zsh

REAL_NAME="Joseph Garvin"

cmd=$(echo git clone https://$LOGNAME@"$CNAME"git/"$PNAME_LOWER"/"$PNAME_LOWER".git)
echo "Running:" "$cmd"
eval "$cmd"
cd "$PNAME_LOWER"
git config user.email "$LOGNAME"@"$CNAME""trading.com"
git config user.name "$REAL_NAME"
