#!/usr/bin/env zsh

# Super dumb script to discover best -j setting for a given machine/source-tree.

set -e

export MAKEFLAGS=""

for i in {1..8}; do
    make distclean
    make distclean
    time make -j$i 2>&1 | tee ~/makelog.time.$i
done
