#!/bin/sh

if test "$REAL_GDB_LOCATION" = ''; then
    echo '$REAL_GDB_LOCATION' "not set! Can't run gdb." >&2
    exit 1
fi

cmd="$HOME/etc/utils/trapper.py '(gdb)' $REAL_GDB_LOCATION $@"

echo "Starting uninterruptible gdb. See file $0."
echo "Running: $cmd"
eval $cmd
