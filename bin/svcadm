#!/usr/bin/env zsh

real_loc=$(cat $HOME/etc/real_locations/$(hostname)/svcadm)

cmd=("PYTHONPATH=$HOME/etc/python python " \
    "$HOME/etc/utils/ctrl-c-handler.py " \
    "safeguard $real_loc $@")

if [[ "$SCRIPTING_DEBUG" = "1" ]]; then
    echo "Running: $cmd"
fi

eval $cmd
