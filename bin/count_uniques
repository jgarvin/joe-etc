#!/usr/bin/env python

import sys

f = sys.argv[1]

countmap = {}
for line in open(f).readlines():
    if line in countmap:
        countmap[line] += 1
    else:
        countmap[line] = 1

for line in countmap:
    print "Line: %s Count: %s" % (line, countmap[line])

